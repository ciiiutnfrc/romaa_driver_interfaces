# Paquete `romaa_driver_interfaces` para ROS 2

Paquete con interface para el nodo driver del robot RoMAA-II.

## Servicios
- `SetOdometry.srv`: modifica el valor de odometría, recibe la pose 2D y devuelve un booleano.
